name: empty
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  faker: ^2.1.0
  flutter_svg: ^2.0.17
  camera: ^0.11.1
  image_picker: ^1.0.7
  path_provider: ^2.1.1
  permission_handler: ^11.0.1 # Para gestionar permisos en Android/iOS
  video_player: ^2.9.2
  audioplayers: ^6.1.2
  mockito: ^5.4.5

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
